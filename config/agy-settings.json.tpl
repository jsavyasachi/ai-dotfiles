{
  "enableTelemetry": false,
  "permissions": {
    "allow": [
      "command(awk)",
      "command(cat)",
      "command(clojure)",
      "command(diff)",
      "command(find)",
      "command(git)",
      "command(grep)",
      "command(head)",
      "command(ls)",
      "command(rg)",
      "command(sed)",
      "command(stat)",
      "command(tail)",
      "command(tree)",
      "command(wc)",
      "read_file(*)"
    ]
  }
}
